-implementa keyManager
-prova a fare una fork nella funz che elimina le entry
-prova a fare in modo che il server cerchi lo spazio libero con una specie di funz di hashing (saltando di qua e di là) se trova tutto occupato al primo giro

-in server, MIGLIORA la funz per chiudere tutti i file descriptor ecc usando gli exit handler (e inoltra SIGTERM)
-crea signal handler per inoltrare SIGTERM anche a KeyManager
-valuta di collassare le 2 funz in server per creare semafori in una sola
	-prova a metterla in semaphores.c

-valuta di creare keyManager in un altro punto di server per non fargli ereditare i file descriptor di FIFOSERVER ecc
-sarebbe figo cercare dove scrivere in shm tramite multithreading (=crea processi figlio per la ricerca)