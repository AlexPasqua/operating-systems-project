Dato che le chiavi dei semaphore sets e degli shared memory segments sono utilizzate solo per la get, è inutile che le ritorni se ho già una funz che crea la chiave e fa subito la get, anche perché poi non sono mai più utilizzate